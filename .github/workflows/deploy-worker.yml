name: Deploy Worker

on:
  push:
    branches:
      - main
      - master
    paths:
      - "worker/**"
      - "worker.js"
      - "scripts/build-worker.mjs"
      - "scripts/worker-build-lib.mjs"
      - "vite.worker.config.js"
      - "frontend/**"
      - "package.json"
      - "package-lock.json"
      - "wrangler.toml"
      - ".github/workflows/deploy-worker.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-worker-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install Dependencies
        run: npm ci

      # 关键步骤：在 CI (Linux) 上补齐 Tailwind v4 的 Linux 原生二进制包，不写入 package.json
      - name: Fix Tailwind Oxide for Linux CI
        run: npm install --no-save @tailwindcss/oxide-linux-x64-gnu

      - name: Build frontend static assets
        run: npm run build:frontend

      - name: Deploy to Cloudflare Workers
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy
          # 填入你的构建产物输出目录（如 frontend/dist）
          directory: frontend/dist
